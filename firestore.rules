rules_version = '2';

service cloud.firestore {
    match /databases/{database}/documents {

        // --- FUNÇÕES AUXILIARES ---
        
        function isAuth() { return request.auth != null; }
        
        // Busca o perfil do usuário logado
        function getProfile() {
            return get(/databases/$(database)/documents/users/$(request.auth.uid)).data;
        }
        
        // Verifica se o usuário pertence à congregação
        function isOfCongregation(congId) {
            return getProfile().congregationId == congId;
        }

        // Verifica se o usuário é Administrador
        function isAdmin() {
            return getProfile().role == 'Administrador';
        }

        // Verifica se o usuário tem cargo de gestão (Admin, Dirigente, Servo)
        function isManager() {
            return getProfile().role in ['Administrador', 'Dirigente', 'Servo de Territórios', 'Ajudante de Servo de Territórios'];
        }

        // --- REGRAS POR COLEÇÃO ---

        match /congregations/{congId} {
            allow read: if isAuth(); 
            allow create: if isAuth();
            
            allow update: if isAuth() && isOfCongregation(congId) && (getProfile().status == 'ativo' || isManager());
            allow delete: if isAuth() && isOfCongregation(congId) && isAdmin();

            // Histórico de Auditoria: Apenas Administradores podem ler/listar
            match /auditLogs/{logId} {
                allow read: if isAuth() && isOfCongregation(congId) && isAdmin();
                allow create: if isAuth() && isOfCongregation(congId) && getProfile().status == 'ativo';
            }

            match /territories/{terrId} {
                allow read: if isAuth() && isOfCongregation(congId) && (getProfile().status == 'ativo' || isManager());
                
                allow create, delete: if isAuth() && isOfCongregation(congId) && (isAdmin() || getProfile().role == 'Servo de Territórios');
                
                allow update: if isAuth() && isOfCongregation(congId) && (getProfile().status == 'ativo' || isManager());

                match /quadras/{quadraId} {
                    allow read, write: if isAuth() && isOfCongregation(congId) && (getProfile().status == 'ativo' || isManager());
                    
                    match /casas/{casaId} { 
                        allow read, write: if isAuth() && isOfCongregation(congId) && (getProfile().status == 'ativo' || isManager());
                    }
                }
                match /activityHistory/{activityId} { 
                    allow read, write: if isAuth() && isOfCongregation(congId) && (getProfile().status == 'ativo' || isManager());
                }
            }
        }

        match /users/{userId} {
            allow create: if isAuth() && request.auth.uid == userId;
            allow get, list: if isAuth();
            
            allow update: if isAuth() && (request.auth.uid == userId || isManager());
            allow delete: if isAuth() && (request.auth.uid == userId || isAdmin());

            match /notifications/{notifId} {
                allow get, list, update, delete: if isAuth() && request.auth.uid == userId;
                allow create: if isAuth() && (request.auth.uid == userId || isManager());
            }
        }

        match /resetTokens/{tokenId} {
            allow read, delete, create: if true;
        }

        match /feedbacks/{feedbackId} { allow create: if isAuth(); }
    }
}