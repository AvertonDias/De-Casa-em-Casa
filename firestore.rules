rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // --- Funções Auxiliares Seguras ---
    function isAuth() {
      return request.auth != null;
    }

    function getUserData(userId) {
      return isAuth() && exists(/databases/$(database)/documents/users/$(userId)) ? get(/databases/$(database)/documents/users/$(userId)).data : null;
    }
    
    function isUserInCongregation(congId) {
      let userData = getUserData(request.auth.uid);
      return userData != null && userData.congregationId == congId;
    }

    function isSameCongregationAsRequestingUser(targetUserId) {
        let currentUserData = getUserData(request.auth.uid);
        let targetUserData = getUserData(targetUserId);
        return currentUserData != null && targetUserData != null && currentUserData.congregationId == targetUserData.congregationId;
    }
    
    function isRole(role) {
      let userData = getUserData(request.auth.uid);
      return userData != null && userData.role == role;
    }
    
    function isManager() {
      let userData = getUserData(request.auth.uid);
      return userData != null && userData.role in ['Administrador', 'Dirigente', 'Servo de Territórios', 'Ajudante de Servo de Territórios'];
    }
    
    function isDirigenteOrAdmin() {
      let userData = getUserData(request.auth.uid);
      return userData != null && userData.role in ['Administrador', 'Dirigente'];
    }

    function canManageUsers() {
      let userData = getUserData(request.auth.uid);
      return userData != null && userData.role in ['Administrador', 'Dirigente', 'Servo de Territórios', 'Ajudante de Servo de Territórios'];
    }

    // --- Coleções ---

    match /congregations/{congId} {
      allow read: if isUserInCongregation(congId);
      allow write: if isRole('Administrador') && isUserInCongregation(congId);

      match /territories/{terrId} {
        allow read, write: if isUserInCongregation(congId);

        match /quadras/{quadraId} {
          allow read, write: if isUserInCongregation(congId);

          match /casas/{casaId} {
            allow read, write: if isUserInCongregation(congId);
          }
        }

        match /activityHistory/{activityId} {
            allow read, write: if isUserInCongregation(congId);
        }
      }
    }

    match /users/{userId} {
      // Usuário pode criar seu próprio documento (essencial para o cadastro).
      allow create: if request.auth.uid == userId;

      // Usuário pode ler seu próprio perfil. CRÍTICO PARA O FUNCIONAMENTO DO APP.
      allow get: if request.auth.uid == userId;

      // Usuário pode ler o perfil de outros da mesma congregação.
      allow get: if isAuth() && isSameCongregationAsRequestingUser(userId);

      // Usuário pode atualizar seu próprio perfil (nome, whatsapp, etc).
      allow update: if request.auth.uid == userId && 
                      // Impede o usuário de alterar seu próprio papel, status ou ID de congregação.
                      !('role' in request.resource.data) &&
                      !('status' in request.resource.data) &&
                      !('congregationId' in request.resource.data);

      // Dirigentes, Servos e Admins podem ATUALIZAR outros usuários (status, papel).
      allow update: if canManageUsers() && isUserInCongregation(resource.data.congregationId);

      // Admins podem DELETAR outros usuários, mas apenas da sua própria congregação e não a si mesmos.
      allow delete: if isRole('Administrador') && 
                      request.auth.uid != userId && 
                      isUserInCongregation(getUserData(userId).congregationId);
      
      // Permite a listagem de usuários se o requisitante tiver permissão e estiver na mesma congregação.
      allow list: if canManageUsers() && request.query.where.get('congregationId')[2] == getUserData(request.auth.uid).congregationId;


      match /notifications/{notificationId} {
        // Usuário pode ler e escrever suas próprias notificações.
        allow read, write: if request.auth.uid == userId;
      }
    }

    match /feedbacks/{feedbackId} {
      allow create: if isAuth();
      allow read, update, delete: if false; 
    }

    match /resetTokens/{tokenId} {
      allow read, write: if false;
    }
  }
}
