"use client";

// Este arquivo pode ser removido, mas por segurança, vamos apenas esvaziá-lo
// e o link para ele já foi removido do layout.

export default function SettingsPage() {
  return null;
}
